import React from "react";
import PropTypes from "prop-types";
import { View, Text } from "react-native";

const PemieredMovies = (props: any) => {
  return (
    <View>
      <Text>PemieredMovies</Text>
    </View>
  );
};

PemieredMovies.propTypes = {};

export default PemieredMovies;
